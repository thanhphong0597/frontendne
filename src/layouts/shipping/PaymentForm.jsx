import React from "react";
import CheckboxLabels from "../../components/checkbox/CheckboxLabels";

const PaymentForm = () => {
    return (
        <>
            <CheckboxLabels
                onChange={() => {}}
                item="Thanh toán khi nhận được hàng"
            />
        </>
    );
};

export default PaymentForm;
